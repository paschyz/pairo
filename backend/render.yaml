services:
  - type: web
    name: pairo-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -e .
    startCommand: uvicorn pairo.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
